services:
  nginx:
    image: nginx:1.27-alpine
    ports:
      - "80:80"
    volumes:
      - ./infra/nginx/default.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - api
      - web

  api:
    environment:
      QUARKUS_PROFILE: prod
