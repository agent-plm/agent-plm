---
theme: auto
server:
  address: tcp://0.0.0.0:9091/
log:
  level: info
identity_validation:
  reset_password:
    jwt_secret: local-dev-only-change-me-jwt-secret
authentication_backend:
  file:
    path: /config/users_database.yml
access_control:
  default_policy: one_factor
  rules:
    - domain: plm.lvh.me
      policy: one_factor
session:
  secret: local-dev-only-change-me-session-secret
  cookies:
    - name: authelia_session
      domain: plm.lvh.me
      authelia_url: https://plm.lvh.me:9091
      default_redirection_url: https://plm.lvh.me:9091/
storage:
  encryption_key: local-dev-only-change-me-storage-key
  local:
    path: /config/runtime/db.sqlite3
notifier:
  filesystem:
    filename: /config/runtime/notification.txt
